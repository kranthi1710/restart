// KMP algorithm 
learned about prefix, suffix, and how to use kmp algorithm for advanced level strings
checked on linkedin learning courses from our compang site.


KMP Alogirthm
LPS - Longest Prefix suffix - Length of the longest proper prefix that is also a suffix.
1. Build LPS array: 
Ex: Onions ---- LPS=[0,0,0,1,2,0]

still below algorithm is wrong, try to revise it again

var strStr = function(haystack, needle) {
    // build lps array
    let m= needle.length;
    let h= haystack.length;
    let i=0;
    let j= 1;
    let lps=[0];
// this below while loop checks only for needle
    while(j<m){
        if(needle[i]===needle[j]){
            lps[j]=i+1;
            ++i;
            ++j;
        } else{
            if(i===0){
                lps[j]=0;
                ++j;
            } else{
            //move i to the previous lps index
                i=lps[i-1]; // ex: aabaaac
            }
        }
    }

    // this below while loop checks for needle and haystack
    i=j=0;
    
    while(i<h){
        if(haystack[i]=== needle[j]){
            ++i;
            ++j;
        } else{
            if(j===0){
                ++i;
            } else{
             lps[j] = lps[j-1];  
            }
        }
    }
    if(j===m){
        return i-m;
    }

    return -1;
    
}

solved best time to buy and sell stock leetcode

