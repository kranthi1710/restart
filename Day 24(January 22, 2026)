Valid Anagram

My Approach using hashing:(Correct conceptually but not ideal) 
/**
 * @param {string} s
 * @param {string} t
 * @return {boolean}
 */
var isAnagram = function(s, t) {
    if(s.length!==t.length){
       return false;
    } 
   let map={};
   for(let i of s){
    map[i]=(map[i] || 0) +1;
   }
   
   for(let i of t){
       if(map.hasOwnProperty(i) && map[i]>=0){
           map[i]--;
       } 
   }


The SUBTLE ISSUE in Your Code
Your current decrement logic:
for (let i of t) {
    if (map.hasOwnProperty(i) && map[i] >= 0) {
        map[i]--;
    }
}

Problem:
If t contains a character not in s, or appears more times, your code:
❌ Does nothing
❌ Continues looping
❌ Only catches it later using Object.values(map)
This is less optimal and less clean.



Correct way:
/**
 * @param {string} s
 * @param {string} t
 * @return {boolean}
 */
var isAnagram = function(s, t) {
    if(s.length!==t.length){
       return false;
    } 
   let map={};
   for(let i of s){
    map[i]=(map[i] || 0) +1;
   }
   
   for(let i of t){
     if(!map[i]) return false; //fail fast
     map[i]--;   
   }
       return true;
       
};
  
   
       return Object.values(map).every(x=>x==0);

       
};



