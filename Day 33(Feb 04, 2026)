// Revise the dsa problems and take codelity test

codelity test --- A react Autocompletion machine coding and c# unit tests usig Moq

My Approach -- Brute force.
var findWordsContaining = function(words, x) {
    let arr=[];
    for(let w=0;w<words.length;w++){
        if(words[w].indexOf(x)!==-1){
            arr.push(w);
        }
    }
    return arr;
};

Brute force --using includes
var findWordsContaining = function(words, x) {
    let arr=[];
    for(let w=0;w<words.length;w++){
        if(words[w].includes(x)){
            arr.push(w);
        }
    }
    return arr;
};

Second approach
var findWordsContaining = function(words, x) {
    let arr=[];
    for(let w=0;w<words.length;w++){
        //find the character in the word without builtin functions
        for(let i=0;i<words[w].length;i++){
             if(words[w][i]===x){
                arr.push(w);
                break;
             }
        }
       
    }
    return arr;
};
