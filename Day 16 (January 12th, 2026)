// Binary search
/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
var search = function(nums, target) {
     let low=0;
    let high=nums.length-1;
   
  while(low<=high){
       let mid=Math.floor((low+high)/2);
        if(nums[mid]==target){
            return mid;
        } else if(nums[mid]<target){
            low=mid+1;
        } else if(nums[mid]>target){
            high=mid-1;
        }
    }
    return -1;
};

Key Binary Search Rules (MEMORIZE)
1️⃣ Works only on sorted arrays
2️⃣ Recalculate mid every iteration
3️⃣ Always move pointers past mid
4️⃣ Loop condition → low <= high

🎤 Interview One-Liner
“Binary search reduces the search space by half on each iteration, giving O(log n) time complexity.”

⏱ Complexity
Time: O(log n)
Space: O(1)



https://leetcode.com/problems/search-insert-position/description/
/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
var searchInsert = function(nums, target) {
    let low=0;
    let high=nums.length-1;
    while(low<=high){
        let mid=Math.floor((low+high)/2);
        if(nums[mid]===target){
            return mid;
        } else if(nums[mid]>target){
            high=mid-1;
        } else if(nums[mid]<target){
            low=mid+1;
        }
    }
    for(let i=0;i<nums.length;i++){
        if(target<=nums[i]){
              return i;
        } 
      
    }
          
          return nums.length;
};

❌ What Can Be Improved (Important)
1️⃣ Extra for loop is unnecessary

After binary search finishes:

for(let i=0;i<nums.length;i++){
    if(target<=nums[i]){
        return i;
    }
}

🚫 This makes time complexity O(n) in worst case
But the problem expects O(log n).

📌 Key insight:
When binary search ends, low already points to the correct insert position.

🧠 Why low is the Answer
When the loop exits:
high < low
All elements before low are < target
All elements from low onward are > target
So the insert position is low.

✅ Optimized & Interview-Perfect Solution
var searchInsert = function(nums, target) {
    let low = 0;
    let high = nums.length - 1;

    while (low <= high) {
        let mid = Math.floor((low + high) / 2);

        if (nums[mid] === target) {
            return mid;
        } else if (nums[mid] > target) {
            high = mid - 1;
        } else {
            low = mid + 1;
        }
    }

    return low;
};
