Missing number
MY Approach - Brute force
/**
 * @param {number[]} nums
 * @return {number}
 */
var missingNumber = function(nums) {
    let map={};
    for(let i of nums){
        map[i]=(map[i] || 0) +1;
    }
    for(let i=0;i<=nums.length;i++){
        if(!map[i]) return i;
    }

};
